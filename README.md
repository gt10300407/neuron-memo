# NEURON MEMO v0.2.1

실제 윈도우95 계열 회색/청록 디자인으로 교체한 수정본이다.

- 새 메모 및 브라우저 저장
- 기존 메모 이어쓰기
- 최초 기록/최종 수정 시간
- 기록 수정 및 이전 내용 이력
- 검색/정렬/삭제
- 뇌지도/다시보기
- JSON 백업/복원
- PC/모바일 반응형

배포:

```bash
cd ~/Downloads && rm -rf NEURON_MEMO && unzip -qo NEURON_MEMO_LATEST.zip && cd NEURON_MEMO && bash publish.sh
```

기존 서비스 워커 캐시 우회용 주소:

```text
https://gt10300407.github.io/neuron-memo/v021.html
```
